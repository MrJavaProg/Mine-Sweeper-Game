#pragma once
ref class CellDrawing
{
public:
	CellDrawing();
	~CellDrawing();

protected:
	//void drawEmptyCell(int %xStart, int %yStart, int %xEnd, int %yEnd);
	/*void drawOpenedCell(int %xStart, int %yStart, int %xEnd, int %yEnd);
	void drawUndefinedCell(int %xStart, int %yStart, int %xEnd, int %yEnd);
	void drawFlaggedCell(int %xStart, int %yStart, int %xEnd, int %yEnd);
	void drawExplodedCell(int %xStart, int %yStart, int %xEnd, int %yEnd);*/
};

